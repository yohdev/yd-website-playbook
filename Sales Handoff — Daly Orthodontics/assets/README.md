# Daly Orthodontics — Asset Drop Zone

Andrew shared the logo and a current-site screenshot in chat on 2026-04-24, but not as files.
Drop them into this folder with the filenames below so Phases 03–05 can reference them directly.

## Needed before Phase 03 (design tokens)
- `logo-primary.png` — the gold-on-black horizontal lockup Andrew pasted (stylized "D" + "DALY" + "Orthodontics").
- `logo-primary.svg` — request SVG source from Victor or Maris. Needed for clean scaling and for theme.json.
- `current-site-home-above-fold.png` — the screenshot Andrew pasted showing the current dailyorthodontics.com hero.

## Nice-to-have before Phase 04 (homepage)
- `logo-mark.svg` — just the stylized "D" (usable as favicon, avatar, OG icon).
- `logo-stacked.svg` — vertical lockup if one exists.
- `logo-white.svg` or `logo-reversed.svg` — for use on dark backgrounds where the gold-on-black is inappropriate (e.g., photo overlays).
- Hex values pulled from the SVG with an eyedropper, to replace the screenshot-approximated values in `../03-Logo-and-Colors.md`.

## Nice-to-have before Phase 02 (research)
- Additional current-site screenshots: mobile nav, Treatments page, New Patients flow, Patient Hub, blog index. These feed the Phase 02 research doc.

## How files are referenced from the playbook
`../03-Logo-and-Colors.md` points at `assets/logo-primary.png`, `assets/logo-primary.svg`, etc. Keep filenames consistent so the playbook's relative links stay valid.
